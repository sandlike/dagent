{
  "name": "opencode-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel --stream run dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "db:push": "pnpm --filter @opencode/server run db:push"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
